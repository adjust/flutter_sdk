name: test_app
description: "Adjust Flutter Test App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0-277.0.dev

dependencies:
  flutter:
    sdk: flutter
  adjust_sdk:
    path: ../../
  test_lib:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
