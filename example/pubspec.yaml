name: adjust_example
description: Example app which demonstrates how to use the adjust_sdk Flutter plugin.

dependencies:
  flutter:
    sdk: flutter
  adjust_sdk:
    path: ../
  cupertino_icons: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
